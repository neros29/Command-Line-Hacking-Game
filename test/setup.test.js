const jest = require('jest');

beforeAll(() => {
  console.log('Setting up test environment');
  // Add any setup code like mocking dependencies
});

afterAll(() => {
  console.log('Cleaning up test environment');
  // Add cleanup code here
});
